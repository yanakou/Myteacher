<% tag_list.each do |tag| %>
  <%= link_to tweets_path(tag_name: tag), style: 'text-decoration: none;' do %>
    <button type="button" class="btn btn-info">
      <%= tag %>
    </button>
  <% end %>
<% end %>
